package datastructures

//case class MultiWayTree[+T](value: T, children: List[MultiWayTree[T]]) {
//  def this(value: T) = this(value, List())
//  override def toString = "M(" + value.toString + " {" + children.map(_.toString).mkString(",") + "})"
//}
//
//object MultiWayTree {
//  def apply[T](value: T) = new MultiWayTree(value, List())
//  def apply[T](value: T, children: List[MultiWayTree[T]]) = new MultiWayTree(value, children)
//}
//
//
//class MultiTree {
//
//object MultiTree {
//  import java.util.concurrent.ConcurrentHashMap
//  val tree = new ConcurrentHashMap[FileInfo, ]()
//}
